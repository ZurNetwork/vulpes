//! The toolkit layer — where credential behavior will live.
//!
//! Deliberately empty (VUL-2): the skeleton exists so later work adds
//! behavior to a place that already compiles, separate from the `v0.1.0`
//! did:plc substrate. The wrapped foundation it will build on is
//! [`crate::vc`] (behind the `vc` feature).
//!
//! The name predates the broker→toolkit language ruling; renaming is open
//! the moment the first real code lands here.
